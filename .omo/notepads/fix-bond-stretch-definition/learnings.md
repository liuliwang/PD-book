# Learnings — fix-bond-stretch-definition

Conventions, patterns, and successful approaches discovered during work on this plan.

_Auto-scaffolded by /start-work. Append new entries below - never overwrite._

---

## 2026-08-03: bond stretch upgraded to numbered equation (ch02 line 73)

- Edited `chapters/ch02_framework.tex` line 73: replaced inline `$s=(|\boldsymbol{\xi}+\boldsymbol{\eta}|-|\boldsymbol{\xi}|)/|\boldsymbol{\xi}|$` with `\begin{equation}` block + `\label{eq:bond-stretch}`, and removed the redundant inline repetition at paragraph end (now references `\eqref{eq:bond-stretch}`).
- Term unified from 键的伸长（stretch） → 键的伸长率（bond stretch）; paragraph end now reads 伸长率由式\eqref{eq:bond-stretch}给出.
- Grep acceptance: `\label{eq:bond-stretch}`=1, `键的伸长（stretch）`=0, `键的伸长根据`=0, `伸长率由式`=1. File grew 873→878 lines (+5).
- Only line 73 area changed; fig:bond-kinematics ref, TikZ figure (lines 80+), and eq:reaction/eq:central-force untouched.

## 2026-08-03: fig:bond-kinematics caption updated to reference eq:bond-stretch (ch02 line 124)

- Edited `chapters/ch02_framework.tex` line 124 caption: `键伸长定义为 $s=(...)` → `键伸长率 $s$ 由式\eqref{eq:bond-stretch}定义`.
- Grep acceptance: `键伸长率 $s$ 由式`=1 (caption), `键伸长定义为`=0, `eq:bond-stretch`=3 total (label 76 + eqref 78 + caption 124). Only the caption line changed.

## 2026-08-03: compile gate PASSED (task 3)

- `xelatex -interaction=nonstopmode -halt-on-error main.tex` twice: exit1=0 exit2=0.
- Second-pass main.log: 0 hits for `LaTeX Error|undefined|Warning: Reference` — first-pass forward-ref warning for eq:bond-stretch resolved on second pass (standard 2-pass behavior, not a failure).
- main.aux now contains `\newlabel{eq:bond-stretch}{{2.1}{16}{键}{equation.2.1}{}}`; fig:bond-kinematics newlabel caption reads 键伸长率 $s$ 由式\eqref{eq:bond-stretch}定义; `键伸长定义为` = 0 matches in aux.
- Evidence written to `.omo/evidence/fix-bond-stretch-definition/task-3-qa.txt`. main.pdf 104 pages.

## 2026-08-03: F4 scope-fidelity acceptance gate PASSED (final gate)

- Ran all 6 required greps on `chapters/ch02_framework.tex` (Grep tool, count mode).
- Counts: `键的伸长（stretch）`=0, `键的伸长根据`=0, `\label{eq:bond-stretch}`=1, `键伸长定义为`=0, `伸长率由式`=1, `键伸长率 $s$ 由式`=1 — all match plan requirements.
- Cross-ref check: `eq:bond-stretch` appears exactly 3× (line 76 \label, line 78 body \eqref, line 124 caption \eqref).
- VERDICT: PASS. Transcript written to `.omo/evidence/fix-bond-stretch-definition/f4-scope-fidelity.txt`.

## 2026-08-03: PLAN SCOPE COMPLIANCE AUDIT (read-only) — PASS

- Method: `git status --short`, `git diff --stat`, `git diff -- chapters/`, targeted greps. No files modified.
- **File classification** (uncommitted tree):
  - `chapters/ch02_framework.tex` — this plan's change (hunks @ -25,7+70,12 = line 73→78 equation block; @ -71,7+121,7 = line 124 caption) **+** pre-existing prior-session changes (hunk @ -13,7+13,52 = fig:material-point-comparison TikZ; hunk @ -129,7+179,7 = `xshift=5.5`→`xshift=5.5cm` bug fix in fig:boundary-truncation, done before this plan started).
  - `AGENTS.md`, `.omo/boulder.json` — pre-existing / plan-machinery, not product files.
  - `main.pdf`/`main.aux`/`main.log`/`main.toc` — compile artifacts, regenerated by task 3 xelatex, not committed.
  - `frontmatter/notation.tex`, `main.tex`, `preamble.tex`, `bibliography.bib` — NO diff, untouched. ✓
- **Guardrail greps** (ch02_framework.tex): `\label{eq:bond-stretch}`=1 ✓; `键的伸长（stretch）`=0 ✓; `键的伸长根据`=0 ✓; `键伸长定义为`=0 ✓; `伸长率由式`=1 (line 78) ✓; `键伸长率 $s$ 由式`=1 (line 124) ✓.
- **notation.tex**: `eq:bond-stretch`=0, not in diff ✓.
- **TikZ check**: hunk for fig:bond-kinematics caption (@ -71,7+121,7) changes ONLY the `\caption` line; `\end{tikzpicture}` above it is context (unchanged). No TikZ content modified inside fig:bond-kinematics. The only in-diff TikZ deltas are the pre-existing fig:material-point-comparison addition and the pre-existing xshift=5.5cm fix — neither from this plan.
- VERDICT: **PASS** — the plan modified ONLY ch02_framework.tex (lines 73-78 + 124) plus `.omo/` artifacts; all guardrails held.

## 2026-08-03: Code quality review — LaTeX syntax of modified regions (task 4)

VERDICT: **PASS**

Check items (read-only review, no files modified):
- Lines 74–77 equation block: `\begin{equation}` … `\end{equation}` correctly closed; `\label{eq:bond-stretch}` on line 76 inside the environment, after the formula, before `\end{equation}` — correct LaTeX placement. ✓
- `\eqref{eq:bond-stretch}` in body (line 78) and caption (line 124) matches the label exactly (case-sensitive, identical spelling). Grep: `eq:bond-stretch` = 3 hits total (76 label, 78 eqref, 124 eqref). ✓
- Formula line 75 `s = \frac{|\boldsymbol{\xi} + \boldsymbol{\eta}| - |\boldsymbol{\xi}|}{|\boldsymbol{\xi}|}` mathematically identical to the replaced inline `(|\boldsymbol{\xi}+\boldsymbol{\eta}|-|\boldsymbol{\xi}|)/|\boldsymbol{\xi}|` — same numerator/denominator, whitespace only. ✓
- Caption line 124: single `\caption{...}` with balanced braces; `\eqref` valid inside caption; `labelsep=quad` → no ASCII colon (only full-width `：` present, which is PRE-EXISTING — old caption had the same full-width colon per git diff, and matches pre-existing caption style at ch02 line 198). Plan only replaced the inline formula with `$s$ 由式\eqref{eq:bond-stretch}定义`. ✓
- No orphaned `\left.`/`\right.` or unmatched delimiters: grep zero hits. ✓
- Chinese flow line 73→78: 「…键的伸长率（bond stretch）定义为」→ equation → 「其推导已在第 1 章 1.4 节给出，此处不再重复。…伸长率由式\eqref{eq:bond-stretch}给出。」 Coherent, no dangling 「根据计算」. ✓
- Notation consistency: `frontmatter/notation.tex` line 21 uses 伸长率（stretch）— same primary Chinese term 伸长率 as new text 伸长率（bond stretch）; English gloss variant acceptable. No change needed. ✓

Non-blocking observations (NOT failures, outside plan's modified regions):
- ch02 line 267 (pre-existing, section 2.2) still carries an inline full formula `$s=(|\boldsymbol{\xi}+\boldsymbol{\eta}|-|\boldsymbol{\xi}|)/|\boldsymbol{\xi}|$` — a reuse of the stretch definition rather than a second definition. Candidate for a future follow-up to reference `\eqref{eq:bond-stretch}`, but not part of this plan and not a syntax issue.

---

## 2026-08-03: Visual PDF QA — VERDICT: PASS

Method: Extracted typeset pages 15–18 from `main.pdf` (physical pages ~24–27, where Chapter 2 resides) via `pdftotext -layout`. The Chapter 2 region is clearly delimited by the header "第 2 章 近场动力学基本理论框架" (line 266 of full-text extraction). Verified against `.aux` metadata: `\newlabel{eq:bond-stretch}{{2.1}{16}{键}{equation.2.1}{}}`, `\newlabel{fig:bond-kinematics}{{2.2}{17}{...}}`.

### Check (a): Equation 2.1 — PASS

Rendered as a *displayed, right-numbered equation*:

```
键的伸长率（bond stretch）定义为

                           |ξ + η| − |ξ|
                      s =                                      (2.1)
                                |ξ|
```

Post-equation body text: "伸长率由式(2.1) 给出。" — flow confirmed.

### Check (b): Figure 2.2 caption — PASS

Caption renders as:

```
图 2.2   键的运动学：参考构型与变形构型中的键几何关系，键伸长率 s 由式 (2.1) 定义
```

- Terminology: "键伸长率" (not "键伸长")
- References "式 (2.1)" correctly

### Check (c): No residual old terms near 2.1.2 — PASS

Full PDF text searched (104 pages). Results:

- In Chapter 2, section 2.1.2 region: 0 hits for "键的伸长（stretch）", "键伸长定义为", "键的伸长定义为". All occurrences use "键的伸长率".
- The single occurrence of "键的伸长（stretch）定义为" in the entire PDF is in Chapter 1, section 1.4 (typeset page ~5–6), a pre-existing introductory paragraph NOT subject to this plan's fix.
- Other occurrences of bare "键的伸长" (lines 1227, 2190, 2444) are technical shorthand in later sections (2.4/2.5/2.7) where "伸长" is used as an established term after the formal definition — all in context, not residual old text.

### Summary

| Check | Requirement | Actual | Verdict |
|-------|-------------|--------|---------|
| (a) | Displayed numbered eq (2.1) | `\begin{equation}` → (2.1) at right margin | PASS |
| (b) | Caption: 伸长率 + 式(2.1) | 键伸长率 s 由式 (2.1) 定义 | PASS |
| (c) | No old terms near 2.1.2 | 0 hits for old terms in Ch2 2.1.2 region | PASS |

Evidence: `.omo/evidence/fix-bond-stretch-definition/pdf-p20-30.txt` (pdftotext extraction), `.omo/evidence/fix-bond-stretch-definition/pdf-full.txt` (full-book extraction for residual-term audit).
